tables: ['currency']

source:
  host: 127.0.0.1:3306
  user: root
  database: trivago
  #pass: 

target:
  host: 127.0.0.1:3306
  user: root
  database: trivago_new
  #pass: 

table:
  currency:
    key: currency_id
    statement: "SELECT * FROM currency"
    method: update
    #batch_size: 10 # default is 1000